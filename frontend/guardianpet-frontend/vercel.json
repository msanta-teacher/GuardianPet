{
  "version": 2,
  "builds": [
    { "src": "angular.json", "use": "@vercel/static-build",
      "config": { "distDir": "dist/guardianpet-frontend" } }
  ],
  "routes": [
    { "src": "/(.*)", "dest": "/index.html" }
  ]
}
